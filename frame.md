# 实施完整方案的路线规划

## 阶段一：基础能力准备（1–2周）

### 目标
补齐方案必需的基础组件，确保训练闭环可行。

### 关键任务

#### 1. 构建可微分ESDF距离场
- 利用障碍物真值位置，在仿真环境中预计算静态ESDF栅格（或利用MuJoCo原生SDF功能实现实时查询）
- 封装ESDF查询模块，支持输入任意点坐标返回距离值及梯度（供PyTorch自动微分）
- 验证模块可微分性：对查询点求导，观察梯度是否合理

#### 2. 实现Dashing软约束成本函数
- 将ESDF距离与Dashing二次惩罚公式结合，编写碰撞成本函数
- 集成光滑性成本、终点成本（如果意图包含目标点）
- 测试成本函数在简单轨迹上的计算与梯度流动

#### 3. 确定意图空间设计
- 从简单开始：仅输出目标点（相对或绝对坐标），必要时归一化
- 扩展维度：后续逐步加入路径偏好（2维）、姿态（4维）等，初期先锁定低维
- 明确意图与YOPO输入的接口方式（如特征拼接）

### 预期产出
- 可微分ESDF查询模块
- Dashing成本函数代码（含梯度测试）
- 意图空间定义文档及初步输入接口

### 风险与对策
- **ESDF构建可能耗时**：可先用少量障碍物验证方案，再扩展
- **ESDF查询成为训练瓶颈**：考虑简化距离计算（如直接障碍物列表计算），待后期优化

---

## 阶段二：YOPO双头改造与预训练（2–3周）

### 目标
将现有YOPO单头生成器改为双头结构，并预训练使其具备基本生成能力。

### 关键任务

#### 1. 网络结构调整
- 保留原有多对多头（预测K条候选轨迹的偏移量和成本）
- 新增一对一 head，直接输出最终轨迹参数（如多项式系数或离散点）
- 两个头共享底层特征提取器（CNN+状态/意图编码器）

#### 2. 预训练数据准备
- 收集随机意图 \( h \) 及对应的图像、状态（可在仿真中随机采样）
- 利用特权信息（ESDF）为每条候选轨迹计算真实成本，作为多对多头的监督信号
- 对一对一 head，同样用真实成本监督，但可给予较小权重或逐步引入

#### 3. 预训练执行
- 仅优化YOPO部分（不涉及RL），最小化多对多头平均成本 + 一对一 head成本
- 监控轨迹生成质量（碰撞率、光滑性），确保预训练后的YOPO能对任意意图生成合理轨迹

#### 4. 双头验证
- 对比预训练前后一对一 head 的输出与多对多头的最佳候选，观察一致性
- 确保梯度能通过一对一 head 反向传播到特征提取器

### 预期产出
- 改造后的双头YOPO网络代码
- 预训练好的YOPO模型（参数可加载）
- 预训练报告：不同意图下的轨迹可视化、成本收敛曲线

### 风险与对策
- **多对多头可能主导训练，导致一对一 head 学不好**：可初期固定多对多头或使用梯度停止技巧，逐步放开
- **预训练数据覆盖不足**：扩大随机意图采样范围，或采用课程采样（先简单后复杂）

---

## 阶段三：上层RL与YOPO联合训练基础版（3–4周）

### 目标
将上层RL与预训练的YOPO结合，在固定YOPO情况下训练RL策略，验证意图决策的有效性。

### 关键任务

#### 1. 修改RL环境
- 将动作空间改为意图向量（替换原有低维控制动作）
- 调整观测：确保RL可获取必要信息（如自身状态、目标位置等），图像可暂不输入RL（由YOPO使用）
- 重写奖励函数：移除避障惩罚，只保留时间惩罚（-0.01/步）和到达奖励（+100）

#### 2. 集成预训练YOPO
- 在RL环境中，调用YOPO生成器（固定参数），输入当前图像、状态、RL输出的意图，得到最终轨迹
- 取轨迹第一步作为控制指令传递给仿真环境

#### 3. 训练RL策略
- 采用PPO算法，在中等难度环境中训练，观察RL是否能学会输出合理意图（如目标点接近实际目标）
- 记录任务成功率、平均步数等指标

#### 4. 评估与调试
- 可视化意图分布及对应生成轨迹，确保RL未输出极端危险意图（YOPO应能安全处理，但仍需监控）
- 若训练不稳定，调整RL超参数或简化意图空间（如只输出目标点）

### 预期产出
- 适配意图动作的RL环境代码
- 训练好的基础RL策略（固定YOPO）
- 训练曲线及初步分析报告

### 风险与对策
- **RL初期探索可能输出极端意图，导致YOPO生成不可行轨迹**：可通过限制意图范围（如目标点不能太远）或使用行为克隆初始化RL
- **RL完全不收敛**：检查奖励设计是否合理，或先降低环境难度

---

## 阶段四：联合微调与渐进损失平衡（2–3周）

### 目标
放开YOPO参数，与RL联合训练，并引入ProgLoss动态调整成本权重。

### 关键任务

#### 1. 启用YOPO参数更新
- 将YOPO生成器设置为可训练，与RL策略一起优化
- 在每次更新时，除了RL损失，还计算YOPO辅助损失（多对多头平均成本 + 一对一 head成本）

#### 2. 实现渐进损失平衡
- 定义 \( \lambda_{\text{coll}}(t) \) 和 \( \lambda_{\text{task}}(t) \) 随时间变化的函数（如分段常数或指数衰减）
- 在损失函数中动态应用权重
- **注意**：\( \lambda_{\text{task}} \) 可与RL奖励中的任务目标协同，避免重复加权

#### 3. 联合训练调试
- 密切监控碰撞率、任务成功率、各项损失的变化
- 如果训练震荡，尝试梯度裁剪、降低学习率，或分阶段更新（先RL几步，再YOPO几步）
- 评估一对一 head 输出质量是否随训练提升

#### 4. 消融实验
- 比较固定YOPO vs 联合微调的性能差异
- 比较有/无ProgLoss的稳定性

### 预期产出
- 联合训练代码（含ProgLoss调度）
- 训练稳定的最终模型（上层RL+下层YOPO）
- 性能对比报告

### 风险与对策
- **联合训练初期YOPO可能被RL带偏，导致生成轨迹质量下降**：可先冻结YOPO一段时间，再逐步解冻
- **ProgLoss权重变化过快可能引起训练不稳定**：需平滑过渡（如线性插值）

---

## 阶段五：课程学习集成与后期优化（长期）

### 目标
引入课程学习，逐步提升任务难度，使策略适应复杂环境，并优化推理性能。

### 关键任务

#### 1. 设计课程难度层级
- **环境难度**：障碍物密度从0.01→0.1/m²，增加动态障碍物、狭窄通道等
- **意图空间**：从仅目标点 → 目标点+路径偏好 → 完整意图（姿态、时间预算）
- **YOPO参数**：碰撞惩罚系数从高到低，原语数量从少到多，轨迹长度从短到长

#### 2. 实现课程调度器
- 根据策略表现（如平均回报、成功率）自动推进难度
- 支持手动预设难度阶段及触发条件

#### 3. 分阶段训练
- 从最低难度开始训练，达到指标后提升难度，继承之前策略权重
- 在每个难度阶段，可能需重新调整ProgLoss参数或RL超参数

#### 4. 部署优化
- 推理时移除多对多头及相关计算，仅保留一对一 head
- 可选：量化模型、减小CNN尺寸，进一步提升实时性

### 预期产出
- 课程学习训练流程及调度器代码
- 能够应对复杂环境的最终策略模型
- 部署版本（轻量推理）

### 风险与对策
- **难度切换可能引起性能退化**：需保存检查点以便回退
- **课程设计需与仿真环境紧密结合**：确保环境参数可动态调整

---

## 路线图总结

| 阶段 | 时间预估 | 核心产出 | 里程碑 |
|------|---------|---------|--------|
| 一 | 1–2周 | ESDF模块、Dashing成本、意图定义 | 基础组件就绪 |
| 二 | 2–3周 | 双头YOPO网络、预训练模型 | YOPO具备基本生成能力 |
| 三 | 3–4周 | 基础RL策略（固定YOPO） | 验证意图决策可行性 |
| 四 | 2–3周 | 联合训练模型、ProgLoss实现 | 端到端系统稳定运行 |
| 五 | 长期 | 课程学习、部署优化 | 高性能可部署系统 |

---

## 关键成功要素

1. **保持模块化设计**，便于各阶段独立调试
2. **重视指标监控**（碰撞率、成功率、损失曲线），快速定位问题
3. **逐步增加复杂度**，避免一步到位导致失控

按此规划推进，你将能系统地构建出一个融合高层RL决策、底层可微分轨迹生成、并具备渐进学习能力的智能规划系统。
